apiVersion: apps/v1
kind: DaemonSet
metadata:
  labels:
    app: local-volume-provisioner
  name: local-volume-provisioner
  namespace: ldp
spec:
  selector:
    matchLabels:
      app: local-volume-provisioner
  template:
    metadata:
      labels:
        app: local-volume-provisioner
    spec:
      containers:
      - env:
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: spec.nodeName
        image: 192.168.137.1/p8/local-directory-provisioner:0.0.1
        command:
        - /usr/local/bin/local-directory-provisioner
        - -v=4
        name: provisioner
        volumeMounts:
        - mountPath: /mnt/ssd
          mountPropagation: HostToContainer
          name: fast-disks
      volumes:
      - hostPath:
          path: /mnt/ssd
          type: ""
        name: fast-disks
      serviceAccountName: local-directory-provisioner
  updateStrategy:
    type: RollingUpdate